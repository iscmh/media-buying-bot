/**
 * Versioned Privacy Policy. Bump on any user-visible content change.
 * Rendered at /legal/privacy (public, read-only). Same versioning
 * discipline as TOS_VERSION but no in-app re-acceptance gate — Privacy
 * is disclosure, not consent.
 *
 * Format: ISO date, letter-suffix if multiple revisions land the same
 * day (e.g. `2026-07-28b`).
 */
export const PRIVACY_VERSION = '2026-07-28';
