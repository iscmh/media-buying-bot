/**
 * Polish-21.0.15: single source of truth for the shipped Polish
 * version. Imported by:
 *
 *   - packages/jobs (worker → composite-row metadata + cold-start log)
 *   - apps/web `/api/health` (curl-verifiable freshness endpoint)
 *   - apps/web `/api/version` (dedicated version endpoint)
 *
 * A shared constant creates a cross-package import chain that MUST
 * rebuild every downstream when this file changes — no more "did
 * Vercel actually pick up the packages/jobs change?" guessing.
 *
 * ---------------------------------------------------------------
 * WHY THIS EXISTS — Polish-21.0.15 root-cause diagnostic
 * ---------------------------------------------------------------
 * Job 961281c5 showed `polish_version: null` on a failed RETRY
 * even though Vercel dashboard confirmed the Polish-21.0.14 SHA
 * (1065775) as deployed. Root cause was likely Inngest's
 * documented behavior: retries of an IN-FLIGHT job execute
 * against the function version that was live when the job was
 * FIRST invoked — not the currently-deployed version. So a job
 * originally submitted pre-Polish-21.0.14 keeps running the old
 * code on every retry.
 *
 * Diagnosis protocol for a "did the deploy stick?" report:
 *   1. Curl `/api/health` — reports POLISH_VERSION. If the value
 *      isn't the SHA-in-question, the deploy legitimately didn't
 *      stick (rare — check Vercel build logs).
 *   2. If /api/health shows the expected version but composite
 *      rows still say polish_version=null, submit a NEW job (not
 *      a retry). Inngest's frozen-version-per-job behavior means
 *      retries never see new code.
 *   3. To force an in-flight job onto the new code: cancel it in
 *      the Inngest dashboard and re-invoke — Inngest treats that
 *      as a new submission and picks up the current function
 *      version.
 */

/**
 * Bumped on every Polish release. Format: MAJOR.MINOR.PATCH.
 * Change here → next `pnpm build` cascades through @mbb/jobs +
 * apps/web (both `transpilePackages`-linked to @mbb/shared).
 *
 * Polish-25.1 (MINOR bump from 25.0.x) marks the UX-layer overhaul
 * shipping in Commit 10a + 10b. The pipeline / worker / BYOK
 * plumbing (Commits 1-9) is untouched; only the presentation +
 * information-architecture layer changes.
 */
export const POLISH_VERSION = '26.0.14';

/**
 * Optional short human-readable slug that pairs with the version
 * for at-a-glance context in the /api/health response. Update
 * alongside POLISH_VERSION when the release ships a materially
 * different fix pattern.
 */
export const POLISH_RELEASE_NAME =
  "Polish-26.0.14 Commit 62.4 hotfix - per-step instrumentation + widened UNDEFINED_VALUE matcher. Real evidence: on POLISH_VERSION 26.0.13 (SHA 8945976) sync run STILL produced bare 'Error: UNDEFINED_VALUE' in Vercel logs with NO [postgres-js UNDEFINED_VALUE at ...] annotation prefix - operator's Commit 62.3 outer try/catch annotator failed to fire OR failed to match. Two possible causes both addressed here: (a) outer catch bypassed by Inngest v3 SDK internals - fix via per-step try/catch INSIDE each step.run callback; (b) matcher didn't recognize the error shape - Inngest v3 wraps step errors in SerializationError classes whose message hides UNDEFINED_VALUE on .cause. Fix layers: (a) new guardedStepRun helper in packages/jobs/src/lib/assert-no-undefined-for-postgres.ts - wraps every step.run with pre-run BEFORE log marker + inner try/catch inside callback + outer try/catch around step.run. Both catches log raw error shape and call rethrowWithUndefinedContext with step-scoped context. Per-step guard fires even when outer function catch is bypassed. (b) looksLikeUndefinedValueError now walks .cause chain up to depth 5 (cycle-safe), matches on: Error.message containing UNDEFINED_VALUE (postgres-js + Inngest v3 shapes), err.code === 'UNDEFINED_VALUE' (postgres-js object shape), Error.name === 'SerializationError' + message contains 'undefined' (Inngest v3 wrapper), bare undefined thrown value (rare but real: `throw undefined`). Annotation prefix widened from '[postgres-js UNDEFINED_VALUE ...]' to just '[UNDEFINED_VALUE ...]' since both driver + serializer produce the same code. (c) safeErrorShape helper produces JSON-safe snapshot of any thrown value - preserves name/message/code/stack (truncated at 800 chars)/cause (recursive). Bare undefined/null flagged with __thrownValue field. Primitives get {__thrownValue, type} envelope. Used by rethrow's raw-log line + outer catch's diagnostic. (d) refresh-heygen-avatar-index.ts rewires every step.run to guardedStepRun (fetch-avatar-list, diff-against-index, mark-disappeared-deleted, preflight-gemini-key, analyze-persist-chunk-N, touch-fresh-rows, resolve-refresh-user x2). Outer function catches log unique markers OUTER-CATCH-CRON-M14 + OUTER-CATCH-MANUAL-M14 + full safeErrorShape dump - if those strings appear in Vercel logs the outer catch definitely fired; if only inner-catch markers appear then Inngest bypassed outer. (e) rethrow ALWAYS logs raw shape via console.error before matching - proves the function was reached even when annotation is skipped due to unrelated error. Regression pin: packages/jobs/tests/guarded-step-run.test.ts +25 assertions across widened matcher (bare 'UNDEFINED_VALUE', err.code shape, .cause chain 4 levels deep, SerializationError class + undefined message, bare undefined throw, false-positive guard, always-logs raw shape) + safeErrorShape (Error with cause chain, bare null/undefined, err.code preserved, stack truncation, primitive envelopes) + guardedStepRun (success passthrough, BEFORE marker log, unrelated errors passthrough, UNDEFINED_VALUE annotation with step: prefix, inner-catch fires on step-callback throw, original error reachable via .cause chain after annotation). Do-not-touched: postgres-js driver, Inngest client config, Drizzle write records (still guarded by Commit 62.3 asserts), stripUndefined + stripUndefinedDeep + safeInngestStepReturn (unchanged - they cover Inngest step-return serializer, orthogonal to postgres-js). Version bump: POLISH_VERSION 26.0.13 -> 26.0.14. IGNORE-PRIOR-NOTES-BELOW-THIS-LINE (Commit 62.3 notes retained). Prior Commit 62.3 - postgres-js UNDEFINED_VALUE root-cause fix + comprehensive audit. Real evidence: three prior commits (61.8 / 62.1 / 62.2) patched Inngest's UNDEFINED_VALUE (step-result serializer). Sync worker on POLISH_VERSION 26.0.12 (SHA 68aa89e) STILL failed with UNDEFINED_VALUE, stack trace Socket.emit + addChunk in Vercel bundle chunk 3703. Root-cause misdiagnosis: the project uses `postgres` (postgres-js by porsager, packages/db/src/client.ts line 2) as its DB driver behind Drizzle. postgres-js throws its OWN UNDEFINED_VALUE from src/query.js: `if (value === undefined) throw errors.generic('UNDEFINED_VALUE', 'Undefined values are not allowed')` — triggered when any query parameter is undefined (top-level in .values / .set, an argument to eq() / inArray(), or a WHERE-clause value). Stack trace's Socket.emit + addChunk was postgres-js's binary-protocol writer (net.Socket.write path), NOT Inngest's step-result serializer. Fix: (a) new packages/jobs/src/lib/assert-no-undefined-for-postgres.ts with FOUR helpers: assertNoUndefinedForPostgres(record, ctx) - tripwire that throws BEFORE postgres-js sees the record, names offending field + full snapshot (undefined marked as __UNDEFINED__); assertArrayNoUndefinedForPostgres(arr, ctx) - for inArray inputs, returns filtered defined-only; assertScalarDefinedForPostgres(v, name, ctx) - for pre-eq() / loadDecryptedKeys() guards; rethrowWithUndefinedContext(err, ctx) - top-level annotator that detects postgres-js UNDEFINED_VALUE by message AND err.code and re-throws with caller context + .cause preserved. Non-UNDEFINED errors re-thrown unchanged. (b) refresh-heygen-avatar-index.ts wires guards at every Drizzle write site: chunk upsert (skip avatars with missing avatar_id + log; assertNoUndefinedForPostgres on the record with `refresh-heygen:persist-avatar:${id}` context), mark-disappeared-deleted (assertArrayNoUndefinedForPostgres on toSoftDeleteIds + assertNoUndefinedForPostgres on set record), touch-fresh-rows (same guards). Entry-point userId guard: assertScalarDefinedForPostgres at refreshHeygenAvatarIndexCore top + defensive typeof/empty-string check throwing NonRetriableError. (c) Both outer inngest.createFunction bodies wrapped in try/rethrowWithUndefinedContext with 'refreshHeygenAvatarIndexCron' + 'refreshHeygenAvatarIndexManual' contexts — postgres-js UNDEFINED_VALUE now surfaces as '[postgres-js UNDEFINED_VALUE at refreshHeygenAvatarIndexCron] ...' instead of opaque 'Undefined values are not allowed'. Regression pin: packages/jobs/tests/assert-no-undefined-for-postgres.test.ts +17 assertions: pass-through clean record; throw with field name + context on undefined; JSON snapshot includes __UNDEFINED__ markers; keeps null/0/''/false/NaN (not the failure vector); array guard names offending indices; scalar guard names field + context; rethrow annotates message-match errors + code-match errors; re-throws unrelated errors unchanged; preserves original via .cause; end-to-end: userId undefined at entry + avatarId undefined in chunk upsert both caught with actionable diagnostics. Do-not-touched: polish26_heygen generation worker (previously patched for Inngest UNDEFINED_VALUE, no postgres-js write paths outside metadata jsonb which JSON.stringify strips), refresh-makeugc-avatar-index (out of scope until symptom observed there), Meta launcher, other pipelines. Prior Inngest-side helpers (stripUndefined / stripUndefinedDeep / safeInngestStepReturn) unchanged - they cover Inngest's serializer, orthogonal to postgres-js's driver. Version bump: POLISH_VERSION 26.0.12 -> 26.0.13. IGNORE-PRIOR-NOTES-BELOW-THIS-LINE (Commit 62.2 notes retained). Prior Commit 62.2 hotfix - void-step-return UNDEFINED_VALUE in sync worker. Real evidence: fresh HeyGen sync run on POLISH_VERSION 26.0.11 STILL failed with UNDEFINED_VALUE (stack trace: Socket.emit + addChunk = Inngest's step-serializer socket). Commit 62.1's deep-strip covered every path where an object with nested undefined keys crosses a step boundary - but did NOT cover the case where the ENTIRE step return is bare undefined. Root cause: two void-callback steps in refresh-heygen-avatar-index (mark-disappeared-deleted + touch-fresh-rows) had `async () => { await db.something(); }` bodies with no explicit return. The promise resolved to bare undefined; JSON.stringify(undefined) returns undefined (not a JSON scalar); Inngest v3 flags exactly that at the step-result boundary. Commit 62.1's deep-strip walker starts with `if (v === undefined) return undefined` - it faithfully passes through bare undefined, which was the wrong behavior for step returns specifically. Fix: (a) new safeInngestStepReturn in packages/jobs/src/lib/strip-undefined.ts that COMBINES the deep-strip (Commit 62.1) with a void-sentinel coercion - if the value is bare undefined, returns the { _void: true } INNGEST_VOID_STEP_RESULT sentinel (JSON-safe, grep-friendly); anything else goes through stripUndefinedDeep. Wrapper is idempotent - safeInngestStepReturn(safeInngestStepReturn(x)) === safeInngestStepReturn(x). (b) refresh-heygen-avatar-index.ts wires safeInngestStepReturn at EVERY step.run return in the sync worker: fetch-avatar-list (avatars array), diff-against-index (buckets), mark-disappeared-deleted (was void - now returns { softDeleted }), preflight-gemini-key ({ ok:true }), analyze-persist-chunk-N (chunkResult - was stripUndefinedDeep, now safeInngestStepReturn for consistency), touch-fresh-rows (was void - now returns { touched }), final summary (was stripUndefinedDeep, now safeInngestStepReturn). Convention: every step callback return goes through the sanitizer, so a future edit accidentally returning undefined is caught at the wrapper rather than at Inngest's UNDEFINED_VALUE. Regression pin: packages/jobs/tests/safe-inngest-step-return.test.ts +8 assertions: bare undefined -> INNGEST_VOID_STEP_RESULT sentinel; JSON.stringify of the sentinel is a real JSON string not undefined (the exact check Inngest performs); object returns deep-stripped (composes 62.1); primitives (string / number / boolean / 0 / '') pass through; null preserved (meaningful JSON); arrays of objects deep-stripped; end-to-end chunk-return shape (nested geminiStatus:undefined vector from 62.1) sanitizes cleanly with JSON.stringify sanity check for no residual undefined anywhere in the tree; void-step return shape ({softDeleted:N} / {touched:N}) serializes cleanly for Inngest. Do-not-touched: polish26_heygen generation worker (Commit 61.8/62.1 already covered), refresh-makeugc-avatar-index (out of scope), Meta launcher, other pipelines, stripUndefined + stripUndefinedDeep primitives (unchanged - safeInngestStepReturn is a composition helper on top). Version bump: POLISH_VERSION 26.0.11 -> 26.0.12. IGNORE-PRIOR-NOTES-BELOW-THIS-LINE (Commit 62.1 notes retained). Prior Commit 62.1 hotfix - deep undefined-strip in refresh-heygen-avatar-index sync worker. Real evidence: Inngest run of HeyGen sync on POLISH_VERSION 26.0.10 failed with UNDEFINED_VALUE. Same error class as Commit 61.8 which only patched the polish26_heygen GENERATION worker's avatar-match step; the SYNC worker (refresh-heygen-avatar-index) has its own Drizzle writes + step.run returns not covered by that fix. Root cause: Commit 62 introduced FailureSample carrying a nested `diagnostics` dict sourced from analyzeMakeugcAvatarThumbnail. That analyzer spreads keys like `geminiStatus: result.status` which can be undefined when the underlying HTTP response has no status field - Commit 61.8's shallow stripUndefined stops at the top level so the undefined-nested-inside-diagnostics key survives, and Inngest's step-result serializer throws UNDEFINED_VALUE. Fix: (a) new stripUndefinedDeep in packages/jobs/src/lib/strip-undefined.ts - recursive walk, drops every undefined-valued key at every level, preserves null / 0 / '' / false / NaN (meaningful JSON), preserves array shape with per-element recursion, passes through non-plain objects (Date, Buffer, class instances) untouched via Object.getPrototypeOf check, cycle-safe via WeakSet. Shallow stripUndefined kept for Commit 61.8's use cases (single-level Drizzle .set() etc.). (b) refresh-heygen-avatar-index.ts wires stripUndefinedDeep at every step.run() return boundary + FailureSample construction: chunk-result return (analyze-persist-chunk-N), final summary return (refreshHeygenAvatarIndexCore). (c) FailureSample construction now explicit `?? null` on avatarId/avatarName/errorMessage + deep-strip on diagnostics before landing in FailureSample - defense in depth. (d) Drizzle upsert record built once with explicit `?? null` on every nullable column (hairStyle / facialHair / wardrobeStyle / wardrobeSummary / backgroundSetting), then run through shallow stripUndefined before .values() / .set() - Drizzle never sees undefined regardless of analyzer output shape. Regression pin: packages/jobs/tests/strip-undefined-deep.test.ts +11 assertions: drops top-level undefined; drops nested undefined (the raison-d'etre); keeps null; keeps falsy-but-defined (0, '', false, NaN); recurses into arrays and cleans object elements; preserves array shape (no positional shift); passes through Date / Buffer / class instances; empty input; cycle-safe on self-referential shapes; does not mutate input; handles the operator-reported FailureSample shape without leaking undefined (JSON.stringify sanity check). Do-not-touched: polish26_heygen generation worker (Commit 61.8 already covered), Meta launcher, other pipelines. Version bump: POLISH_VERSION 26.0.10 -> 26.0.11. IGNORE-PRIOR-NOTES-BELOW-THIS-LINE (Commit 62 notes retained). Prior Commit 62 - HeyGen avatar sync schema resilience + diagnostics. Real evidence: prior sync (Commit 61.4-era) circuit-breaker tripped at chunk 44 after 20 consecutive 'JSON did not match schema' failures, persisting only 422 avatars of ~1264 live and leaving ~842 unindexed (of which post-Commit-61.7 background allowlist only 97 passed matcher gate). Two root causes: (1) strict Zod rejected common Gemini enum drift (south_asian for ethnicity, grey for hair_color, numeric age like '45' instead of bucket '40s', 'office' for background_setting) - equivalent labels the matcher would have accepted fine, (2) all schema-fails hashed to the constant string 'JSON did not match schema' so 20 distinct drift patterns collapsed to one circuit-breaker bucket and tripped the breaker in 20 chunks. Fix: (a) new parseMakeugcAvatarVisionAnalysisWithDiagnostics in packages/jobs/src/lib/makeugc-avatar-vision-prompt.ts - strict-parse first, then conservative coercion (mapping tables for AGE_BUCKET_ALIASES / ETHNICITY_ALIASES / HAIR_COLOR_ALIASES / FACIAL_HAIR_ALIASES / WARDROBE_STYLE_ALIASES / BACKGROUND_ALIASES + numeric age -> bucket floor via /10, range parser '40-49' -> 40s, freeform string truncation to schema max lengths, empty confidence_note fill), then diagnostic-fail with per-field signature. Only unambiguous equivalents mapped - genuinely unknown values still fail so operator sees them. (b) packages/jobs/src/functions/refresh-heygen-avatar-index.ts wires the diagnostic parser + splits failure signatures by root cause: schema:<fields> vs mime-filter:<mime> vs image-fetch:<status> vs gemini:<status> vs gemini:empty-response vs skip:<reason> vs concurrency-runner-error. Twenty different drift patterns now hash to twenty different signatures - won't collectively trip the breaker. (c) DISTINCT_FAILURE_SAMPLE_CAP = 5 per-signature samples captured on the summary (first occurrence of each signature wins), each carrying avatarId + previewUrl + errorMessage + diagnostics + schemaFieldIssues + schemaRawSnapshot (400-char JSON snapshot of what Gemini emitted). Operator now sees every root cause in one sync run instead of only the first-ever failure. (d) CROSS_CHUNK_CONSECUTIVE_FAILURE_THRESHOLD bumped 20 -> 40 for headroom now that same-signature collapse is prevented at source. (e) new analyzeCoerced counter on summary - how many successful analyses needed the coercion layer (elevated count => refine strict prompt). Regression pin: packages/jobs/tests/heygen-vision-coercion.test.ts +21 assertions: strict path unchanged, coercion covers ethnicity/hair_color/age/facial_hair/wardrobe_style/background_setting drift, over-long strings truncated to schema max, missing confidence_note filled, coercionsApplied surfaced, hard-fail returns split signatures per field, rawSnapshot capped at 400 chars, composite operator-suspected chunk-44 drift shape parses cleanly. Do-not-touched: strict parseMakeugcAvatarVisionAnalysis (still exported for MakeUGC sync + existing tests), refresh-makeugc-avatar-index (out of scope), HeyGen v3 client, HeyGen worker, Meta launcher, other pipelines. Version bump: POLISH_VERSION 26.0.9 -> 26.0.10. IGNORE-PRIOR-NOTES-BELOW-THIS-LINE (Commit 61.9 notes retained for context but superseded). Prior Commit 61.9 hotfix - gate Polish-23-era nested-quote validator per pipeline; polish26_heygen opts out. Real evidence: fresh Polish-26 HeyGen generation on concept ddca175b failed with 'Polish-26 condensed-script validation failed [nested-quotes]'. Real: source-video vision analysis contained natural nested-quote pattern (\"we'll see\"); Claude condenser correctly preserved the dialogue verbatim as the Polish-25/26 system prompt instructs. Real: containsQuotedThirdPartySpeech (Polish-23 Commit 3.0.23) was built to defend Google Veo's TTS from nested-quote artifacts - HeyGen's TTS tolerates them fine, real evidence: MakeUGC + HeyGen sample scripts with nested quotes render cleanly on the HeyGen side. Fix (Option A - per-caller gate, cleanest): (a) checkPolish25CondensedScript in packages/jobs/src/lib/polish25-claude-script-condenser-prompt.ts now takes a Polish25ScriptCheckOptions second arg with { skipNestedQuotes?: boolean } - default false preserves the Polish-25 MakeUGC behavior. (b) polish26_heygen worker call site (packages/jobs/src/functions/generate-polish26-heygen.ts line ~354) now passes { skipNestedQuotes: true } - narrow, explicit, self-documented at the call site. (c) All other checks (empty, too-long, appearance-leak) apply UNCONDITIONALLY regardless of the flag - the appearance-leak invariant especially cannot be weakened because the pre-cast avatar visualizes appearance so the script must never describe it, HeyGen or otherwise. Regression pin: packages/jobs/tests/polish25-script-check-gate.test.ts +9 assertions: default (no options) still rejects nested quotes; explicit {} still rejects; skipNestedQuotes:false still rejects; skipNestedQuotes:true accepts nested quotes; skipNestedQuotes:true still rejects empty / too-long / appearance-leak; clean scripts pass in both modes. Do-not-touched: HeyGen client, sync worker, Meta launcher, static pipelines, polish25 MakeUGC worker (still rejects nested quotes by default), Polish-23 Veo worker, Zod schema Polish25CondensedScriptSchema (leave rejecting nested quotes - only the imperative check gained the option). Version bump: POLISH_VERSION 26.0.8 -> 26.0.9.";

/**
 * Frozen at module-load time so cold-start diagnostics have a
 * stable value even if a caller mutates process.env mid-execution.
 * Consumed by /api/health for release-SHA reporting when the
 * platform sets VERCEL_GIT_COMMIT_SHA (Vercel does by default).
 */
export const POLISH_RELEASE_SHA: string =
  process.env['VERCEL_GIT_COMMIT_SHA'] ??
  process.env['GIT_COMMIT_SHA'] ??
  '(unknown — set VERCEL_GIT_COMMIT_SHA or GIT_COMMIT_SHA)';
